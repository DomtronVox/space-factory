#ifndef KILLABLE_PART_H
#define KILLABLE_PART_H

using namespace std;

class KillablePart {
//variables
    int maxHealth;
    int curHealth;

//functions
    hit();
    die();
};

#endif // KILLABLE_PART_H
